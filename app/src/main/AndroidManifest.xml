<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarPooling"
        tools:targetApi="31">
        <activity
            android:name=".model.view.LogIn.LogInActivity"
            android:exported="false" />
        <activity
            android:name=".model.view.SignInActivity"
            android:exported="false"
            android:label="@string/title_activity_sign_in"
            android:theme="@style/Theme.CarPooling" />
        <activity
            android:name=".model.view.SighUpActivity"
            android:exported="false" />
        <activity
            android:name=".model.view.SignUpActivity"
            android:exported="false"
            android:label="@string/title_activity_sign_up"
            android:theme="@style/Theme.CarPooling" />
        <activity
            android:name=".ui.login.SignUpActivity"
            android:exported="false"
            android:label="@string/title_activity_sign_up" />
        <activity
            android:name=".FrontPageActivity"
            android:exported="false"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.CarPooling" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CarPooling">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>